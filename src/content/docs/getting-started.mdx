---
title: Getting Started
description: Install saQut, understand the supported platforms, and learn the structure of a saQut project.
---

This guide covers getting the compiler onto your machine, which platforms are
supported, and the basic structure of a saQut project. To start writing code
first, go to [Hello World](/hello-world/).

## Get saQut

You have two options: build from source, or download a pre-built release.

### 1. Download a release (recommended)

Pre-built binaries are available for the following platforms:

| Platform | Architecture | Status |
|---|---|---|
| Linux (all distributions) | x86-64 | Supported |
| Windows 11 | x86-64 | Supported |

Grab the latest version from [GitHub Releases](https://github.com/saqutlang/saqut/releases).
Extract the archive, then run the binary directly:

```bash
./saqut --help
```

No installer, no system-wide dependencies. The binary is self-contained.

### 2. Build from source

If you prefer to compile saQut yourself, you need **C++17**, **CMake >= 3.16**,
and **Ninja**.

```bash
git clone https://github.com/saqutlang/saqut
cd saqut
cmake -B build -G Ninja
cmake --build build
```

The binary lands at `build/saqut`.

```bash
./build/saqut --help
```

You should see a list of available commands.

## Supported platforms

saQut targets Linux x86-64 and Windows 11 x86-64. There is no platform-specific
code in the compiler; it uses only standard C++17 and portable libraries. ARM
(aarch64) and other architectures are planned for a later release but are not
shipped yet. macOS is not a target and will not be supported.

If you run into issues on an untested platform, [open an
issue](https://github.com/saqutlang/saqut/issues) with your OS, architecture,
and compiler version.

## Program structure

A saQut program is a list of **function definitions** and **global variable
declarations**. There is no mandatory `class` or boilerplate; execution starts
at a function called `main`.

```c
// Function definitions come first
int greet(string name) {
    print("Hello");
    print(name);
    return 0;
}

// Then the entry point
int main() {
    greet("saQut");
    return 0;
}
```

Each statement ends with `;`. Blocks are enclosed in `{ }`.

## The `print()` function

`print()` is a built-in **host function**: it is implemented in C++ inside the
compiler itself, not in saQut code. It accepts a single argument of any type
and writes it to the terminal:

```c
print(42);           // integer
print(3.14);         // float
print("text");       // string
print(true);         // boolean, outputs "1" or "0"
```

## What's next?

- Write your [first program](/hello-world/) from scratch
- Learn about [variables](/variables/) and how to store and name data
- Explore [data types](/data-types/) to see what kinds of values exist
- Try the interactive `saqut exec` command to experiment with expressions
